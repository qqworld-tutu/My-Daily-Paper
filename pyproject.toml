[project]
name = "daily-paper-push-mvp"
version = "0.1.0"
description = "Daily paper push system MVP skeleton"
requires-python = ">=3.11"

[project.scripts]
daily-paper-push = "src.scheduler.daily_job:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
